# Secrets / credentials
.env
.env.*
!.env.*.template
.vercel.live.env
klaviyo-config.json
secrets/

# Vercel local cache/link
.vercel/

# Claude Code personal settings. Permission grants are per-machine and per-person, not
# team policy, so this file stays out of the repo.
.claude/settings.local.json

# Dependencies
node_modules/

# Logs
*.log
local-debug.*
local-serve.*
local-email-audit.*
serve-local-wrapper.*
vercel-dev*.log
vercel-dev-session.log

# Temp / scratch / debug artifacts
tmp/
tmp-*/
tmp-*.png
tmp-*.mp4
_tiny_test.mp4
_write_ok.txt
_login_headers.txt
_live_snapshot_*.json
*.tmp
westpack-fr-check.png

# Test output
test-results/

# Local-mode Campaign Studio and Content Agent state. These are per-machine runtime files that
# the Redis backend replaces in production; committing them would publish an operator's drafts.
data/campaign-studio-drafts/
data/content-agent-state.json
data/content-agent-state.lock.json
data/content-agent-controls.json
data/content-agent-controls.lock.json
data/campaign-audit-log.json
