# Meta Token Test

1. Copy `meta-config.template.json` to `meta-config.json`
2. Fill in:
   - `appId`
   - `adAccountId`
   - `accessToken`
3. Tell Codex the file is ready
4. Codex can then run `meta-test.ps1` to verify the token against Meta

Do not post the token in chat.
